import { IconToggleDirective } from './icon-toggle.directive';

describe('IconToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new IconToggleDirective();
    expect(directive).toBeTruthy();
  });
});
